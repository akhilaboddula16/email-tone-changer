[project]
name = "mail-toner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.2.0",
    "langgraph>=1.0.5",
    "langgraph-cli[inmem]>=0.4.11",
]
